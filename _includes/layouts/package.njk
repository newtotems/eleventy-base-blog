---
layout: layouts/base.njk
templateClass: tmpl-post
---

{% set packid = page.filePathStem | replace ("/packs/","") %}

{% for item in packages %}
{% if item.packageid == packid %}

{% for customer in customers %}
{% if customer.customerid == item.customer%}
{% set fname = customer.firstname %}
    <h1 class="row d-print-none">Here's whats in this month's pack, {{fname}}</h1>

{% include "layouts/label.njk" %}

{% endif %}
{% endfor %}

<section class="row blog-post d-print-none">
{% for unit in stock %}
    {% for mask in item.contents %}
       {% if unit.stockid == mask %}

<div class="col-4">
    image
</div>
    <div class="col-8 text-left">
            <h2>{{unit.name}}</h2>
            <p>{{unit.description}}</p>
    </div>
        {% endif %}
    {% endfor %}
{% endfor %}
</section>

  {% endif %}
{% endfor %}